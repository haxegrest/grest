package grest.container.v1.types;
typedef ReleaseChannel = {
	/**
		channel specifies which release channel the cluster is subscribed to.
	**/
	@:optional
	var channel : grest.container.v1.types.ReleaseChannel_channel;
}