package grest.firebasedynamiclinks.v1.types;
typedef NavigationInfo = {
	/**
		If this option is on, FDL click will be forced to redirect rather than show an interstitial page.
	**/
	@:optional
	var enableForcedRedirect : Bool;
}