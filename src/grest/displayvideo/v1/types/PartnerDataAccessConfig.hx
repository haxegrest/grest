package grest.displayvideo.v1.types;
typedef PartnerDataAccessConfig = {
	/**
		Structured Data Files (SDF) settings for the partner. The SDF configuration for the partner.
	**/
	@:optional
	var sdfConfig : SdfConfig;
}