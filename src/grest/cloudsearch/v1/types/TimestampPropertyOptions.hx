package grest.cloudsearch.v1.types;
typedef TimestampPropertyOptions = {
	/**
		If set, describes how the timestamp should be used as a search operator.
	**/
	@:optional
	var operatorOptions : TimestampOperatorOptions;
}