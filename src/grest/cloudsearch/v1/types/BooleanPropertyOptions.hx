package grest.cloudsearch.v1.types;
typedef BooleanPropertyOptions = {
	/**
		If set, describes how the boolean should be used as a search operator.
	**/
	@:optional
	var operatorOptions : BooleanOperatorOptions;
}