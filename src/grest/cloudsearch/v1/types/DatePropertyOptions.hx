package grest.cloudsearch.v1.types;
typedef DatePropertyOptions = {
	/**
		If set, describes how the date should be used as a search operator.
	**/
	@:optional
	var operatorOptions : DateOperatorOptions;
}