package grest.cloudsearch.v1.types;
typedef DoublePropertyOptions = {
	/**
		If set, describes how the double should be used as a search operator.
	**/
	@:optional
	var operatorOptions : DoubleOperatorOptions;
}