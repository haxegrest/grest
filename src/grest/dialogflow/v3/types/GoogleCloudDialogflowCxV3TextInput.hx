package grest.dialogflow.v3.types;
typedef GoogleCloudDialogflowCxV3TextInput = {
	/**
		Required. The UTF-8 encoded natural language text to be processed. Text length must not exceed 256 characters.
	**/
	@:optional
	var text : String;
}