package grest.cloudidentity.v1.types;
typedef ModifyMembershipRolesResponse = {
	/**
		The `Membership` resource after modifying its `MembershipRole`s.
	**/
	@:optional
	var membership : Membership;
}