package grest.docs.v1.types;
typedef LinkedContentReferenceSuggestionState = {
	/**
		A mask that indicates which of the fields in sheets_chart_reference have been changed in this suggestion.
	**/
	@:optional
	var sheetsChartReferenceSuggestionState : SheetsChartReferenceSuggestionState;
}