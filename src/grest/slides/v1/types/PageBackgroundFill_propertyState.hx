package grest.slides.v1.types;
@:enum abstract PageBackgroundFill_propertyState(String) from String to String to tink.Stringly {
	var INHERIT = "INHERIT";
	var NOT_RENDERED = "NOT_RENDERED";
	var RENDERED = "RENDERED";
}