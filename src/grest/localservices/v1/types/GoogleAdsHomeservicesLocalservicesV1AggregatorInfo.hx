package grest.localservices.v1.types;
typedef GoogleAdsHomeservicesLocalservicesV1AggregatorInfo = {
	/**
		Provider id (listed in aggregator system) which maps to a account id in GLS system.
	**/
	@:optional
	var aggregatorProviderId : String;
}