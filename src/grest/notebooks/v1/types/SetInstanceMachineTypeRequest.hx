package grest.notebooks.v1.types;
typedef SetInstanceMachineTypeRequest = {
	/**
		Required. The [Compute Engine machine type](https://cloud.google.com/compute/docs/machine-types).
	**/
	@:optional
	var machineType : String;
}