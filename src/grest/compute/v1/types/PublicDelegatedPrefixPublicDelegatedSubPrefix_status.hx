package grest.compute.v1.types;
@:enum abstract PublicDelegatedPrefixPublicDelegatedSubPrefix_status(String) from String to String to tink.Stringly {
	var ACTIVE = "ACTIVE";
	var INACTIVE = "INACTIVE";
}