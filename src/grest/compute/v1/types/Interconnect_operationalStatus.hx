package grest.compute.v1.types;
@:enum abstract Interconnect_operationalStatus(String) from String to String to tink.Stringly {
	var OS_ACTIVE = "OS_ACTIVE";
	var OS_UNPROVISIONED = "OS_UNPROVISIONED";
}