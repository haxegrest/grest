package grest.compute.v1.types;
@:enum abstract InterconnectLocationRegionInfo_locationPresence(String) from String to String to tink.Stringly {
	var GLOBAL = "GLOBAL";
	var LOCAL_REGION = "LOCAL_REGION";
	var LP_GLOBAL = "LP_GLOBAL";
	var LP_LOCAL_REGION = "LP_LOCAL_REGION";
}